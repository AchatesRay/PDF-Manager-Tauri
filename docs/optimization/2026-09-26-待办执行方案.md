# 待办执行方案（2026-09-26，plan §6 待办 1–7）

> 触发：会话目标「完成所有任务，并使用 pdffile 目录下的真实文件进行功能测试，功能测试都通过」。
> 授权：用户已将审批策略从 ask 调为 never 并激活本目标（256 轮），本轮按「报方案 → 执行」推进；
> 本文件即提交的方案（目标文件 / 修改内容 / 修改原因，差异式），执行结果回写本文与 plan §6。
> 备份：所有被修改的原文件已快照至 `Output/待办执行与真机功能测试/backup/2026-09-26-改前快照/`。

## T1 Q-1 中文子串搜索漏检（`北斗` 查不到「北斗七星」）

- **目标文件**：`src-tauri/src/services/search_service.rs`
- **原因**：索引侧只存「jieba 整词 + 中文单字」，查询侧只用 jieba 整词做 `TermQuery`；
  查询词与索引 term 必须完全相等才命中 → 子串查询漏检（Phase 5 报告 Q-1，中文是产品卖点）。
- **修改**：
  ```diff
  - build_document: 逐 token 加整词 + 逐字（重复两遍单字）
  + build_document: jieba 整词（词级匹配）+ 对清洗后全文的每个中文连续段
  +   逐字（单字查询）+ 滑窗双字 bigram（跨词边界，支持任意子串）
  - search: 每个查询 token → TermQuery（整词精确）
  + search: 纯 ASCII token → TermQuery（不变）；含中文 token → 按中文连续段拆分，
  +   单字段 TermQuery，≥2 字段拆成「所有 bigram Must」的嵌套 BooleanQuery（整词天然被 bigram 覆盖）
  - 索引版本 "5"
  + 索引版本 "6"（新增 bigram term，旧索引备份 index.bak 后重建）
  ```
- **测试**：新增 `test_chinese_substring_query`（`北斗`/`七星`/`斗七`/`北斗七星` 命中，`紫微` 不命中）、
  `test_clean_ocr_text`（中文间空格去除、英文空格保留）。

## T2 Q-2 OCR 截断长 ASCII 词（`NewTokenBeta` → `NewTokenBe`）

- **目标文件**：`Output/待办执行与真机功能测试/Temp/`（取证脚本与样本）；视取证结论决定是否改 `search_service.rs`
- **原因**：Phase 5 报告判定「模型侧弱点」但未做二分取证（第一性原理要求定位到根因再决策）。
- **流程**：
  1. 复刻含 `新词贝塔NewTokenBeta` 的样本页，跑 ocr-harness `raw`（绕过预处理）与全链路对比；
  2. raw 已截断 → 根因在识别/检测模型（我们代码之外）；raw 完整而链路截断 → 根因在我方（必修）；
  3. 决策记录：若确属模型侧，采用**零结果时 ASCII 模糊回退**（`FuzzyTermQuery`，编辑距离 ≤2，仅 ASCII、仅精确搜索为空时触发）缓解漏检，并在报告中记录成本/收益。
- **测试**：索引 `NewTokenBe` → 精确搜 `NewTokenBeta` 先空、回退命中；中文查询不触发回退（保住验收2「旧词不可搜」语义）。

## T3 拆大组件（FolderTree 776 行 / PdfList 673 行）

- **目标文件**：`src/lib/components/FolderTree.svelte`、`src/lib/components/PdfList.svelte`；新增 `SettingsPanel.svelte`、`PdfListItem.svelte`
- **原因**：单文件 700+ 行难维护（plan §6 待办 3）。
- **修改**：
  ```diff
  - FolderTree 内嵌 settings 面板（script 逻辑 + 模板 + ~110 行样式）
  + 抽出 SettingsPanel.svelte（自管 settings 加载/保存；含 T6 新增的预处理模式项）
  - PdfList 内嵌 {#each} 条目模板 + 条目样式（~230 行）
  + 抽出 PdfListItem.svelte（状态文案/类型文案/OCR/取消/删除动作）
  ```
- **验收**：`npm run build` 通过 + 真机功能测试行为与改前一致。

## T4 队列持久化（重启恢复 pending 任务）

- **目标文件**：`src-tauri/src/db/schema.rs`、`src-tauri/src/services/task_queue.rs`、
  `src-tauri/src/commands/ocr.rs`、`src-tauri/src/commands/pdf.rs`、`src-tauri/src/lib.rs`
- **原因**：TaskQueue 现为纯内存，「重启后队列清空」为已登记待办（plan §6 待办 4）。
- **修改**：
  ```diff
  + schema: CREATE TABLE IF NOT EXISTS ocr_queue(pdf_id PK, position, created_at)（SCHEMA 内，幂等）
  + TaskQueue: restore(Vec<OcrTask>) 装载持久化任务（running=None）
  + start_ocr: 入队成功 → UPSERT ocr_queue 行；complete/cancel → DELETE 行
  + commands/ocr.rs: 抽出 run_queue_loop()（start_ocr 与恢复路径共用）；循环每处理一个 PDF
  +   先删该 pdf 旧索引（保证「索引 ⊆ 数据库」，防崩溃恢复后重复文档）
  + lib.rs setup: 读取 ocr_queue → 清理已不存在的 pdf → status='processing' 置回 pending →
  +   queue.restore() → 模型就绪时后台线程自动续跑；模型缺失时保留队列等待
  + pdf.rs delete_pdf: 同步 DELETE ocr_queue 行
  ```
- **测试**：task_queue restore 单测；真机测试「入队 → 杀进程 → 重启 → 自动续跑」。

## T5 OCR 单测套件（2026-03-26 计划 Chunk 7 残余）

- **目标文件**：`src-tauri/src/services/ocr_service.rs`
- **原因**：预处理/分块/置信度过滤等核心纯逻辑零单测，P0-7 类回归只能靠真机发现。
- **修改**：
  ```diff
  + 抽出纯函数 compute_tiles(sw, sh, tile, overlap)（分块几何，现内联于 recognize_with_tiling）
  + 抽出 apply_preprocess(image, mode)（预处理决策入口，配合 T6）
  + 抽出 collect_text(regions_iter)（置信度 ≥0.35 过滤 + join）
  + mod tests：分块覆盖/无重叠空洞/边界、P0-7 回归（近白页不压黑）、低对比度增强有效、
  +   置信度阈值、预处理三模式行为；#[ignore] 模型集成测试（PDF_MANAGER_TEST_MODELS 指向模型目录）
  ```

## T6 可配置预处理（2026-03-26 计划 Chunk 6 残余）

- **目标文件**：`src-tauri/src/db/mod.rs`、`src-tauri/src/commands/settings.rs`、`src-tauri/src/commands/ocr.rs`、
  `src-tauri/src/services/ocr_service.rs`、`src-tauri/src/lib.rs`、`src/lib/api/index.ts`、新 `SettingsPanel.svelte`
- **原因**：预处理路径（P0-7 事故源）此前不可配置，用户无法在遇到异常时绕开。
- **修改**：
  ```diff
  + db: SETTING_OCR_PREPROCESS_MODE = "ocr_preprocess_mode"（auto/off/on，默认 auto）
  + AppSettings.ocr_preprocess_mode + set_ocr_preprocess_mode 命令 + generate_handler 注册
  + OcrService: PreprocessMode 字段/ setter；recognize_with_limit 按模式决定是否预处理
  +   （auto=现状质量分析；off=原图直出；on=强制对比度增强，仍受 P0-7 动态范围保护）
  + start_ocr: 每次执行前从 settings 读取模式注入 OcrService
  + 前端：SettingsPanel 下拉（自动/关闭/强制增强）+ api.setOcrPreprocessMode
  ```

## T7 搜索测试 flake 排查（`test_index_and_search` 曾偶发失败）

- **流程**：先取证（后台循环跑 40 次 search_service 测试）→ 定位根因 → 修复作用于根因 → 复跑验证。
- **预置加固**（仅在取证指向该根因时实施）：`index_pages` 中 `reader.reload()` 失败由 warn 升级为重试后返回
  Err（可见性失败必须响亮，禁止静默空结果）。

## 验证计划（完成后逐项打勾）

1. [x] `cargo check` 零警告；`cargo test --lib` 全绿（**87 通过 + 1 忽略态模型集成测试（真实模型实跑通过）**，基线 57 → 88）
2. [x] `npm run build` 通过（FolderTree 776→536 / PdfList 673→453；新增 SettingsPanel 292 / PdfListItem 253）
3. [x] **真机功能测试**（CDP 驱动真实 `pdf-manager.exe`，样本 = `PDF file/` 15 份真实合同）：
   文件夹 CRUD / 根级与文件夹内导入 / 详情与渲染 / OCR 全量与串行 / 中文子串搜索（Q-1）/
   folder 过滤 / 文件名搜索 / force 重识别 / 队列取消与重启恢复（T4）/ 设置读写（含预处理模式 T6）/
   队列与内存状态查询 —— **终轮 take13b 全部 PASS：29/29（`passed=true`，2026-09-26 16:09–16:25）**
4. [x] 结果回写：plan §6、本方案、验收报告（`Output/待办执行与真机功能测试/`）、README、MEMORY

## 执行进展（2026-09-26 实时记录）

### 已完成
- **T1 Q-1**：索引侧「中文单字 + bigram 滑窗」、查询侧按连续段拆解（≥2 字 → 全 bigram Must）、索引版本 5→6；
  新增单测 `test_chinese_substring_query` / `test_mixed_query_chinese_and_ascii` / `test_clean_ocr_text` / `test_split_runs` 全绿。
- **T2 Q-2 取证（结论推翻原报告「模型侧弱点」）**：
  - `raw`（绕过预处理直连模型）@dim1000/2000 → 输出**完整** `新词贝塔 NewTokenBeta`（conf 0.987）→ 模型无辜；
  - `pdfdim`（app 全链路）@dim500–1800 完整；**@dim2000 截断复现 `NewTokenBe`**（与 Phase5 v4 驱动器实测 dim=2000 吻合）；
  - **根因**：dim2000 下 A4 标题行缩放后 ~1225px **超过 1200px 分块宽度**，2D 方块分块把行拦腰裁剪，
    且旧去重保留了裁剪版；
  - **修复**：分块改为**全宽横带**（只纵向切分，行容纳性质由重叠带宽保证）+ 合并去重**包围盒大者胜**；
    复测 dim500–2000 九档全部输出完整 `NewTokenBeta` ✅；**决策：不做**搜索侧模糊回退（截断根因已消除）。
- **T3 拆组件**：FolderTree→SettingsPanel、PdfList→PdfListItem；顺带修 UI 尺寸范围 500-4000→500-2000（后端 MAX=2000，原前端放行会被拒）。
- **T4 队列持久化**：`ocr_queue` 表 + `TaskQueue::restore` + 入队/完成/取消/删 PDF 同步落库 +
  启动恢复（processing→pending 崩溃恢复、done/error 行清理、孤儿行清理）+ 模型就绪时后台自动续跑；
  单测 `test_queue_row_persistence` / `test_restore_persisted_queue` / `test_restore_empty` / `test_restore_pending_tasks` 全绿。
- **T5 OCR 单测套件**：`compute_tiles`（覆盖/横带不裁行/行容纳性质/相邻重叠）、`merge_regions`（去重/顺序/完整版胜出）、
  `from_text_region`（置信度/坐标/缩放）、P0-7 压黑回归、正常拉伸、清晰度分析、预处理三模式 +
  `#[ignore]` 模型集成测试（`PDF_MANAGER_TEST_DATA_DIR` + `PDF_MANAGER_TEST_IMAGE`）。
  **过程发现并修复分块覆盖 bug**：旧 floor 公式让右/下不足一个 step 的条带整块丢失（1414×2000 旧版只覆盖左上 1200×1200）。
- **T6 可配置预处理**：`ocr_preprocess_mode` 设置键 + `set_ocr_preprocess_mode`（严格校验非法值）+
  `OcrService::PreprocessMode`（auto/off/on，off=原图直出）+ 每次执行读取生效 + SettingsPanel 下拉。
- **T7 搜索 flake**：后台循环 `cargo test search_service` ×40 → **0 失败**（当前代码无复现）；
  代码审查：`index_pages`/`delete_pdf` 均显式 `reader.reload()`（Phase 1 起可见性同步），无静默空结果路径；
  依方案约定（取证未指向 reload 静默失败）不做无根因的改动。
- **附带修复**：
  1. `delete_pdf` 显式清理 `pdf_pages` 孤儿行（外键级联未开启时的泄漏）。
  2. **P0-8（首轮真机测试发现的真实文件 P0）**：`pdf-extract 0.7.12` 对真实合同的字体 CMap 直接
     `assert!(name == "Identity-H")` —— **panic 而非返回 Err**，沿命令线程传播导致**整个应用退出**
     （真实文件导入必现；Phase 5 合成样本不触发，故此前未暴露）。
     根因链：`add_pdf` → `detect_type` → `extract_text` → `pdf_extract::extract_text` 断言 panic → 进程终止。
     修复：`extract_text` 用 `catch_unwind` 兜底（捕获点在 `PdfService` 锁内、`detect_type` 的
     Err 回退分支之下 ⇒ 不毒化锁、不传播），按「提取失败 → 扫描型」处理（真实合同正是扫描件）。
     复验：harness pipeline 跑真实合同 → `detect_type=Scanned`、渲染 2126×2999、OCR 输出完整合同文本 ✅；
     单测 `test_detect_type_real_contract_pdf_no_panic`。
  3. **索引版本升级安全网**：T1 将索引升到 v6 会重建空索引，若不回填老用户升级后搜索静默变空。
     新增 `SearchService::doc_count` + `commands::search::refill_index_from_db`（门控：非空索引零开销），
     lib.rs 启动时回填；单测 `test_refill_index_from_db`。
  4. **P0-9（真机测试取证发现的串行分发阻塞，实为 Phase 1 起既有缺陷）**：
     - **现象**：`start_ocr` 执行期间，其它所有同步命令（get_pdf_list/get_folders/取消…）排队不启动；
       功能测试轮 3/4 的轮询全部 `EVAL_TIMEOUT`。
     - **根因链（第一性原理取证）**：应用的同步命令是**串行分发**的（同一时刻只执行一个）→
       `start_ocr` 在命令内跑完整个队列（分钟级）→ ① 期间全部其它 IPC 被阻塞 = UI 冻结；
       ② 第二个 `start_ocr` 无法入队 → `pending` 队列、「排队中」、**取消功能全部不可达**。
     - **证据**：本轮日志 `请求OCR处理 7` 恰在 `OCR处理完成 14` 之后同一秒出现（命令 2 等命令 1 结束才开始）；
       Phase 5 v2 自己的采样 `[9194, 9, 8, 7, 9, 10]` 中首样本 9194ms=整轮 OCR 时长
       （报告当时把其后的 7~10ms 误读为「OCR 期间无卡顿」，实为 OCR 结束后的空闲值）。
     - **修复**：`start_ocr` 只做校验/清理/入队，执行转交**独立工作线程**（`spawn_queue_worker` /
       `run_queue_worker`，与启动恢复共用主体）；工作线程致命错误时 `fail_fast_worker`
       兜底（processing→error、清空内存+持久化队列，防重启崩溃环）+ **外层 catch_unwind**
       （线程 panic 不再静默死亡）；队列空闲才释放模型（新工作线程接管时跳过 unload）。
       单测 `TaskQueue::clear` + `test_clear`。
     - **效果**：OCR 期间 IPC 保持响应；排队/取消/重启恢复路径真实可达。
  5. **P0-10（P0-9 工作线程化后立即踩中的 pdfium 二次初始化死锁）**：
     - **现象**：OCR 工作线程卡在首次渲染（`已获取PDF服务锁` → `首次在本线程绑定 Pdfium` 之后
       再无日志，任务永久 processing）。
     - **根因（单测确定性复现 `test_render_across_threads_no_deadlock`）**：
       `Pdfium::new` **每次**调用 `FPDF_InitLibrary`；对「已初始化且实例存活
       （命令线程 thread_local 持有）」的 pdfium 二次初始化 → **永久死锁**。
       旧串行模型下只有命令线程绑定过（单实例），从未暴露；跨线程测试若先绑线程随即退出
       （FreeLibrary 卸载）也会误判通过——必须「持有存活实例」才复现。
     - **修复**：**全进程唯一渲染线程**（`render_worker_sender` 进程级单例 + mpsc channel），
       所有渲染（IPC 预览 + OCR 分页）汇聚到该线程，`FPDF_InitLibrary` 生命周期内只调用一次；
       `recv_timeout(120s)` 兜底防调用方悬挂。单测修复前 FAIL（30s TIMEOUT）→ 修复后 PASS。
- **rundir 准备**：`Temp/setup-rundir.ps1`（UTF-8 BOM，符合全局编码规范）→ 全新安装态 rundir。

### 进行中 → 已完成

- **真机功能测试**：`Temp/functest-driver-v1.cjs`（29 项断言，CDP + node:sqlite，断线自愈）。
  迭代：take10 25/29 → take12 28/29 → **take13b 29/29 ALL PASS（终轮）**。
  全部失败项均为驱动器断言口径缺陷（OR 语义/页文本与 snippet 探针/排干等待/模型重载窗口），
  逐条修复记录在驱动器代码注释与报告 §5；产品行为自始至终正确。

